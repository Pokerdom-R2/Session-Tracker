# Трекер покерных сессий в Покердом

**Session-Tracker** — это оффлайн-трекер покерных сессий для рума Покердом, созданный для анализа игры по локальным сохранённым раздачам.

---

## 📌 Возможности

- 📁 Чтение локальных файлов раздач Покердома  
- 🔍 Автоматический парсинг каждой раздачи  
- 🧠 Определение:
  - времени раздачи  
  - лимита  
  - результата игрока  
  - размера BB  
- 📊 Группировка раздач в сессии по разрыву во времени  
- 📈 Подсчёт статистики:
  - профит в деньгах  
  - профит в BB  
  - bb/100  
  - длительность сессии  
  - количество раздач  
- 📚 Формирование отчётов:
  - summary (общая сводка)
  - sessions (сессии)
  - limits (по лимитам)
- 💾 Экспорт:
  - CSV
  - JSON

---

## 📁 Структура проекта

```

Session-Tracker/
│
├── tracker.py        # Основной скрипт трекера
├── README.md         # Документация
└── LICENSE           # Лицензия MIT

````

---

## 🚀 Быстрый старт

### 1. Установка Python
Необходимо иметь **Python 3.9+**  
Проверить версию:

```bash
python --version
````

### 2. Клонирование репозитория

```bash
git clone https://github.com/Pokerdom-R2/Session-Tracker.git
cd Session-Tracker
```

### 3. Запуск трекера

Минимальный пример:

```bash
python tracker.py --hands-path ./hands --hero-name MyNickname
```

С рекурсивным обходом папок:

```bash
python tracker.py --hands-path ./hands --hero-name MyNickname --recursive
```

Экспорт отчётов:

```bash
python tracker.py \
  --hands-path ./hands \
  --hero-name MyNickname \
  --export-csv sessions.csv \
  --export-json full_report.json
```

---

## ⚙️ Аргументы командной строки

| Аргумент                  | Описание                                                           |
| ------------------------- | ------------------------------------------------------------------ |
| `--hands-path PATH`       | Путь к папке с файлами раздач (**обязательно**)                    |
| `--hero-name NAME`        | Ник героя (**обязательно**)                                        |
| `--session-gap-minutes N` | Разрыв между раздачами, формирующий новую сессию (по умолчанию 30) |
| `--recursive`             | Рекурсивный обход папок                                            |
| `--all-files`             | Обрабатывать все файлы, а не только `.txt`                         |
| `--encoding ENCODING`     | Кодировка входных файлов (по умолчанию `utf-8`)                    |
| `--export-csv FILE`       | Экспорт отчёта по сессиям в CSV                                    |
| `--export-json FILE`      | Экспорт полного отчёта в JSON                                      |
| `--report ...`            | Какие отчёты выводить: `summary`, `sessions`, `limits`             |

---

## 📊 Типы отчётов

### ▶️ Summary (общая сводка)

* количество раздач
* количество сессий
* общий профит
* общий профит в BB
* общий bb/100
* диапазон дат

### ▶️ Sessions (по сессиям)

Для каждой сессии выводится:

* дата начала
* количество раздач
* лимит
* профит
* профит в BB
* bb/100
* длительность

### ▶️ Limits (по лимитам)

Для каждого лимита:

* количество сессий
* количество раздач
* суммарный профит
* суммарный профит в BB
* bb/100

---

## 💾 Пример экспорта

### CSV

Файл сессий содержит колонки:

```
session_id, start_time, end_time, duration_minutes,
hands_count, limit, total_result_money,
total_result_bb, bb_per_100
```

### JSON

Полная структура включает:

* summary
* sessions
* limits

---

## 🧩 Как работает логика сессий

Сессия формируется автоматически, если:

* разрыв между раздачами меньше указанного порога
* по умолчанию: **30 минут**

Если разрыв больше → начинается новая сессия.

---

## 🛠 Известные ограничения

* Парсер раздач — базовый. Формат раздач в Покердоме нестандартизирован, поэтому для точности рекомендуется адаптировать регулярные выражения под свои файлы.
* Если в раздаче отсутствуют данные (например, время), она будет пропущена.

---

## 📃 Лицензия

Проект распространяется под лицензией **MIT**. См. файл [`LICENSE`](./LICENSE).
